{
  "name": "script-execution-server",
  "version": "1.0.0",
  "description": "This is a simple Node.js backend using Express. It exposes an API endpoint to start a script, accepting a single string argument.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
